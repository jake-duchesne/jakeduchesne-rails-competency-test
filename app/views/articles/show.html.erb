    <p id="notice"><%= notice %></p>


    <div class="col-md-8 blog-main">
        <h2><%= @article.title %></h2>

        <strong>User_ID:</strong>
        <%= @article.user_id %>

        <p><%= @article.body %></p>
        <%= link_to 'Edit', edit_article_path(@article) if logged_in?(:editor) %> | <%= link_to 'Back', articles_path %>
    </div>